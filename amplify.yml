version: 1
backend:
  phases:
    preBuild:
      commands:
        - pnpm install --frozen-lockfile --prefer-offline
    build:
      commands:
        - env | grep -e NEXT_PUBLIC_ >> .env.production
        - pnpm run build
frontend:
  phases:
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - .next/cache/**/*
      - .pnpm-store/**/*
